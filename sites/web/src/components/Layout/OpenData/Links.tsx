import { NavLink } from "react-router-dom";

const OpenDataLinks = (): JSX.Element => (
  <>
    <NavLink to="visualization" className="nav-item">
      VISUALIZATION
    </NavLink>
  </>
);

export default OpenDataLinks;
